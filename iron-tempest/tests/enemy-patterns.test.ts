import { describe,expect,it } from 'vitest';import { fanBurst,formationFor } from '../src/game/EnemyPatterns';
describe('enemy formations',()=>{it('rotates through distinct five-ship formations',()=>{expect(formationFor(0)).toHaveLength(5);expect(formationFor(1)).toHaveLength(5);expect(formationFor(2)).toHaveLength(5);expect(formationFor(0)).not.toEqual(formationFor(1))});it('includes an elite anchor in the crossfire formation',()=>{expect(formationFor(2).some(s=>s.elite)).toBe(true)})});describe('bullet patterns',()=>{it('creates symmetric downward fan bullets',()=>{const fan=fanBurst(7,2);expect(fan).toHaveLength(7);expect(fan[0].vx).toBeGreaterThan(0);expect(fan[6].vx).toBeLessThan(0);expect(fan.every(v=>v.vy>0)).toBe(true)})});
